# allow parallel package installation
options(Ncpus = 4)

# set number of cores used by biocparallel
options(mc.cores = 4)

#### -- Packrat Autoloader (version 0.5.0) -- ####
source("packrat/init.R")
#### -- End Packrat Autoloader -- ####
